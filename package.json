{
  "name": "oba-api-proxy",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "test": "mocha --ui tdd --reporter spec -s 500 -t 10000"
  },
  "engines": {
    "node": "~4.4"
  },
  "dependencies": {
    "compression": "^1.2.0",
    "connect": "~3.3.0",
    "request": "~2.45.0"
  },
  "devDependencies": {
    "async": "^2.1.2",
    "envrun": "0.0.2",
    "mocha": "^3.1.2",
    "nock": "^8.1.0",
    "should": "^9.0.0",
    "supertest": "^2.0.1"
  }
}
